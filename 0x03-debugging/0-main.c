#include "main.h"
/**
 * main - test that the function positive_or_negative() gives the correct
 * output when given a case of 0.
 *Return: 0
 */
void positive_or_negative(int i);
{
    int i;
 
    i = 0;
    positive_or_negative(i);

    return 0;
}
